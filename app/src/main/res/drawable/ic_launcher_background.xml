<?xml version="1.0" encoding="utf-8"?>
<!--
  Launcher background: the app's own dark blue (DarkBlueSurface), so the icon reads as belonging to
  this app rather than as a leftover template. Flat on purpose — the foreground ring carries the
  whole shape, and a patterned background would fight it at 48dp.
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <path
        android:fillColor="#14203C"
        android:pathData="M0,0h108v108h-108z" />
</vector>
