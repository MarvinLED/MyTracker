<?xml version="1.0" encoding="utf-8"?>
<!--
  Themed-icon layer: the same ring in a single colour, since the system tints this and the
  foreground's two-tone track/progress split would collapse into one flat blob if it were reused.
  The gap in the ring is what carries the "progress" reading here.
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <path
        android:fillColor="#00000000"
        android:pathData="M54,27 A27,27 0 1 1 27,54"
        android:strokeColor="#FFFFFF"
        android:strokeLineCap="round"
        android:strokeWidth="10" />
</vector>
